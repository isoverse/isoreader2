% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{ir_plot_continuous_flow}
\alias{ir_plot_continuous_flow}
\title{Plot continuous flow data}
\usage{
ir_plot_continuous_flow(
  dataset,
  panel = file_name,
  color = mass,
  linetype = species,
  colors = palette.colors(),
  scientific = FALSE,
  time_window = NULL,
  short_time_labels = FALSE,
  n_time_breaks = 5,
  n_y_breaks = 5,
  theme = ir_default_theme()
)
}
\arguments{
\item{dataset}{an \code{ir_aggregated_data} object from \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}}
or a plain data frame with a \code{time.s} column and an \verb{intensity.*} column}

\item{panel}{column or expression for faceting (default: \code{file_name}). Set
to \code{NULL} to suppress panels.}

\item{color}{column or expression for the colour aesthetic (default: \code{mass})}

\item{linetype}{column or expression for the linetype aesthetic (default:
\code{species})}

\item{colors}{named or unnamed character vector of colours passed to
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}} (default: \code{\link[=palette.colors]{palette.colors()}})}

\item{scientific}{whether to format y axis labels in scientific notation
(default: \code{FALSE})}

\item{time_window}{optional numeric vector of length 2 giving the time axis
display window \code{c(min, max)} in seconds. Data just outside the window is
retained for correct y autoscaling at the edges;
\code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} clips the display. Default \code{NULL} shows the
full time range.}

\item{short_time_labels}{whether to use compact time axis labels with no
space between value and unit and abbreviated units (\code{hr}, \code{m}, \code{s})
(default: \code{FALSE})}

\item{n_time_breaks}{desired number of time axis tick marks (default: \code{5})}

\item{n_y_breaks}{desired number of y axis tick marks (default: \code{5})}

\item{theme}{ggplot2 theme to apply (default: \code{\link[=ir_default_theme]{ir_default_theme()}})}
}
\value{
a \code{ggplot} object
}
\description{
Plots chromatographic trace data from an \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}} result or
a plain data frame. When \code{dataset} is an \code{ir_aggregated_data} object, the
\verb{$traces} dataset is inner-joined with \verb{$metadata} (bringing in all metadata
columns not already present in \verb{$traces}) before plotting. The plot data must
contain a \code{time.s} column and an \verb{intensity.*} column — an error is thrown if
either is missing. The intensity unit suffix becomes the y axis label.
}
