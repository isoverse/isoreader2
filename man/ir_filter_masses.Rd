% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{ir_filter_masses}
\alias{ir_filter_masses}
\title{Filter isofiles by mass}
\usage{
ir_filter_masses(isofiles, mass)
}
\arguments{
\item{isofiles}{datasets aggregated from \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}}
(\code{ir_aggregated_data}) or a collection of isofiles from \code{\link[=ir_read_isofiles]{ir_read_isofiles()}}
(\code{ir_isofiles})}

\item{mass}{which masses to keep, as a \link[tidyselect:language]{tidyselect}
expression evaluated as if the masses present in the data were column names
\itemize{
\item the same syntax as the \code{mass} argument of \code{\link[=ir_plot_traces]{ir_plot_traces()}}. E.g.
\code{c("44", "45")} or \code{44:48} for specific masses, \code{-"45"}/\code{!"45"} to exclude
one, \code{everything()} for all of them, and helpers such as \code{starts_with("4")},
\code{matches()}, \code{all_of()}, or \code{any_of()}. Unlike plain tidyselect, numbers
select by name rather than by position (\code{44:48} means the masses 44 to 48,
not the 44th to 48th mass). Selecting a mass that is not in the data is an
error that lists the available masses; use \code{any_of()} to ignore missing ones.
}}
}
\value{
the \code{isofiles} object (of the same type as the input) with only the
selected masses
}
\description{
Keeps only the requested masses in the \code{traces}, \code{cycles}, and \code{scans} data of
either an \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}} result (\code{ir_aggregated_data}) or a
collection of isofiles read with \code{\link[=ir_read_isofiles]{ir_read_isofiles()}} (\code{ir_isofiles}). Like
\code{\link[=ir_filter_metadata]{ir_filter_metadata()}} it works on both object types and returns the same type
it was given.
}
\details{
Any metadata record (\code{uidx} + \code{analysis}) that has no data left in \emph{any} of
\code{traces}/\code{cycles}/\code{scans} afterwards is removed from the metadata, and the
removal cascades to the other datasets exactly as in \code{\link[=ir_filter_metadata]{ir_filter_metadata()}}
(\code{resistors} and \code{problems} are restricted to the remaining \code{uidx}). For an
\code{ir_isofiles} object, a file whose metadata ends up empty is dropped from the
collection entirely. Note that this also drops records that never had any
\code{traces}/\code{cycles}/\code{scans} data to begin with (e.g. a file that failed to
read) - check \code{\link[=ir_get_problems]{ir_get_problems()}} before filtering if you need to keep track
of those.

The \code{mass} selection is always resolved \strong{once against the whole object} (all
files of an \code{ir_isofiles} collection, and all of \code{traces}/\code{cycles}/\code{scans} of
an aggregated dataset combined), so selecting a mass that only some of the
files or datasets contain works and simply leaves the others without data.

Ratios calculated with \code{\link[=ir_calculate_ratios]{ir_calculate_ratios()}} live in the \code{ratio_name} /
\code{ratio} columns of the rows of their \strong{numerator} mass, so they are kept or
removed with that mass: filtering to \code{mass = 44} also removes the \code{"45/44"}
ratio (which sits on the mass 45 rows), while \code{mass = c(44, 45)} keeps it.
\code{resistors} are never filtered by mass since they describe the instrument
configuration rather than measured data.

As for the metadata operations, columns that are entirely \code{NA} across all
remaining rows are dropped from every (non-empty) dataset and the
\emph{not-aggregated} column information is cleared.
}
\examples{
\dontrun{
# keep only the CO2 masses
dataset |> ir_filter_masses(44:46)

# keep everything except mass 45
dataset |> ir_filter_masses(-"45")
}
}
