% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{ir_plot_traces}
\alias{ir_plot_traces}
\alias{ir_plot_continuous_flow}
\title{Plot trace data}
\usage{
ir_plot_traces(
  dataset,
  species = NULL,
  mass = everything(),
  ratio = everything(),
  facet = NULL,
  data_type_as_facet = auto(),
  scales = "free",
  nrow = NULL,
  ncol = 1,
  color = color,
  linetype = NULL,
  color_values = palette.colors(),
  drop_unused_levels = FALSE,
  scientific = FALSE,
  time_window.s = if (is.null(time_window.min)) NULL else 60 * time_window.min,
  time_window.min = NULL,
  short_time_labels = FALSE,
  n_time_breaks = 5,
  n_y_breaks = 5,
  ...
)

ir_plot_continuous_flow(...)
}
\arguments{
\item{dataset}{an \code{ir_aggregated_data} object from \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}}
or a plain data frame with \code{species}, \code{time.s}, \code{mass}, and an
\verb{intensity.*} column}

\item{species}{optional vector to filter the displayed data to specific
species (e.g. \code{"CO2"} or \code{c("N2", "CO2")}); default \code{NULL} shows all species.}

\item{mass}{which masses to show as intensity traces, as a
\link[tidyselect:language]{tidyselect} expression evaluated as if the masses
present in the data were column names. The default \code{everything()} shows
every mass and \code{NULL} (or \code{c()}) shows none; beyond that any tidyselect
syntax works, e.g. \code{c("44", "45")} or \code{44:48} for specific masses,
\code{-"45"}/\code{!"45"} to exclude one, and helpers such as \code{starts_with("4")},
\code{matches()}, \code{all_of()}, or \code{any_of()}. Unlike plain tidyselect, numbers
select by name rather than by position (\code{44:48} means the masses 44 to 48,
not the 44th to 48th mass). Selecting a mass that is not in the data is an
error that lists the available masses; use \code{any_of()} to ignore missing ones.}

\item{ratio}{which ratios to additionally show (computed with
\code{\link[=ir_calculate_ratios]{ir_calculate_ratios()}}), as a \link[tidyselect:language]{tidyselect} expression
evaluated as if the ratio names present in the data were column names - the
same syntax as \code{mass}, e.g. \code{everything()} (the default, all available
ratios), \code{NULL} for none, \code{c("45/44", "46/44")}, \code{-"45/44"}, or
\code{starts_with("45")}. Selecting specific ratios when ratios have not been
calculated is an error pointing to \code{\link[=ir_calculate_ratios]{ir_calculate_ratios()}} (with the default
\code{everything()}, or \code{NULL}, and no ratios present, none are simply added).
Ratio rows are plotted on the same \code{value} axis with \code{data_type = "ratios"};
\code{data_type} is then used as a facet row (see \code{data_type_as_facet}) to
separate them from the intensities.}

\item{facet}{column or expression to facet by (default: \code{NULL}, no extra
faceting). When
\code{data_type} is used as a facet row (see \code{data_type_as_facet}), a single
\code{facet} variable becomes the facet_grid column (\code{data_type ~ facet}) and a
\code{NULL} facet gives \code{data_type ~ .}. Otherwise a plain column or expression
(e.g. \code{file_name} or \code{paste(species, mass)}) is faceted with
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}, and a two-sided formula (e.g. \code{species ~ mass}) is
faceted with \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}}. Set to \code{NULL} to suppress faceting.}

\item{data_type_as_facet}{whether the \code{data_type} column (intensities vs
ratios) is used as the \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}} row variable: \code{auto()}
(default) uses it only when more than one data type is present; \code{TRUE}
always uses it; \code{FALSE} never does. When used, the y axis label is dropped
(the facet strip provides it) and the facet becomes \code{data_type ~ .} (a \code{NULL}
\code{facet}) or \code{data_type ~ facet} (a single-variable \code{facet}). It is ignored
when \code{facet} is a two-sided formula (a warning is issued if
\code{data_type_as_facet = TRUE} is combined with a formula \code{facet}, since the two
are mutually exclusive).}

\item{scales}{whether facet scales should be \code{"free"} (default), \code{"fixed"},
\code{"free_x"}, or \code{"free_y"}; passed on to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} /
\code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}}.}

\item{nrow, ncol}{number of rows and columns of facet panels (\code{nrow} default
\code{NULL} lets ggplot2 choose; \code{ncol} default \code{1} stacks the panels in a
single column). Only applies when \code{facet} is a single variable or
expression (faceted with \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}); ignored when \code{facet} is a
formula (faceted with \code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}}), with a warning if you set
them explicitly.}

\item{color}{column or expression for the colour aesthetic (default: the
generated \code{color} column, which holds all the traces that share a species
and (numerator) mass, e.g. \code{"N2: 29, 29/28"} for both the intensity trace
\code{"N2: 29"} and its ratio trace \code{"N2: 29/28"}, so they are drawn in the same
colour while remaining separate lines). The legend for it is labelled
\code{trace}; use \code{color = trace} to give every trace its own colour instead.}

\item{linetype}{column or expression for the linetype aesthetic (default:
\code{NULL}, i.e. no linetype aesthetic)}

\item{color_values}{named or unnamed character vector of colours passed to
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}}, or \code{NULL} to use the ggplot2 default
colour palette (default: \code{\link[=palette.colors]{palette.colors()}})}

\item{drop_unused_levels}{whether to drop unused colour factor levels (e.g.
traces that are absent after zooming to a window) from the colour scale and
legend. Default \code{FALSE} keeps every level so the colour mapping stays stable
across subsets of the same dataset; set to \code{TRUE} to show only the levels
actually present in the plotted data. Note that a \code{color} level covers every
trace of its species/mass, so it is only dropped when none of them is shown.}

\item{scientific}{whether to format y axis labels in scientific notation
(default: \code{FALSE})}

\item{time_window.s, time_window.min}{optional numeric vector of length 2
giving the time axis display window \code{c(min, max)}, either in seconds
(\code{time_window.s}) or in minutes (\code{time_window.min}, converted to seconds
internally — the function always works in seconds). Provide at most one; if
both are given, \code{time_window.s} is used. Must have \code{min < max}. The data
point just outside each edge of the window is retained so the clipped lines
interpolate correctly across the window boundaries and y autoscales correctly
at the edges; \code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} clips the display. A window that
contains no data points of its own is allowed (the line is drawn between the
bracketing points). Default \code{NULL} (both) shows the full time range.}

\item{short_time_labels}{whether to use compact time axis labels with no
space between value and unit and abbreviated units (\code{hr}, \code{m}, \code{s})
(default: \code{FALSE})}

\item{n_time_breaks}{desired number of time axis tick marks (default: \code{5})}

\item{n_y_breaks}{desired number of y axis tick marks (default: \code{5})}

\item{...}{additional arguments passed on to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} or
\code{\link[ggplot2:facet_grid]{ggplot2::facet_grid()}} (e.g. \code{labeller}); for the deprecated
\code{ir_plot_continuous_flow()} all arguments are simply passed on to
\code{ir_plot_traces()}}
}
\value{
a \code{ggplot} object with \code{\link[=ir_default_theme]{ir_default_theme()}} applied. To customize the
plot, add ggplot2 layers on top (e.g. \code{+ ggplot2::theme(...)} or
\code{+ ggplot2::labs(...)}); attach ggplot2 with \code{library(ggplot2)} first.
}
\description{
Plots chromatographic trace data (continuous flow) from an
\code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}} result or
a plain data frame. The data is prepared with \code{\link[=ir_generate_traces_tibble]{ir_generate_traces_tibble()}}
(which, for an \code{ir_aggregated_data} object, inner-joins the \verb{$traces} dataset
with \verb{$metadata}). The plot data must contain \code{species}, \code{time.s}, \code{mass}, and
an \verb{intensity.*} column — an error is thrown if any are missing. A \code{trace}
identifier (\code{"<species>: <mass>"}) and the matching \code{color} identifier are
always regenerated and the plotted \code{value} together with a \code{data_type} label
(\code{"intensity [UNITS]"}, or \code{"ratios"} for ratio rows) are added.

\code{ir_plot_continuous_flow()} is a deprecated alias for
\code{ir_plot_traces()} - it takes the same arguments and returns the same plot.
Use \code{ir_plot_traces()} instead, it is the more flexible name for what the
function actually plots (trace data, which continuous flow files contain).
}
