% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{c.ir_aggregated_data}
\alias{c.ir_aggregated_data}
\title{Combine aggregated isofile data}
\usage{
\method{c}{ir_aggregated_data}(...)
}
\arguments{
\item{...}{\code{ir_aggregated_data} objects to combine}
}
\value{
a single combined \code{ir_aggregated_data} object
}
\description{
Combine multiple \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}} results into a single
\code{ir_aggregated_data} object by row-binding each of the contained datasets
(\code{metadata}, \code{traces}, \code{cycles}, \code{scans}, \code{resistors}, \code{vendor_data_table},
\code{problems}, ...) with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}. Datasets present in only some of
the objects are combined as well (missing columns are filled with \code{NA}). The
file index \code{uidx} is re-numbered across the inputs so each file stays uniquely
identified (and its datasets stay correctly linked) in the combined object.
}
