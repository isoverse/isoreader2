% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intensity.R
\name{ir_convert_intensity}
\alias{ir_convert_intensity}
\title{Convert intensity between units}
\usage{
ir_convert_intensity(
  dataset,
  resistors = NULL,
  units = c("mV", "V", "fA", "pA", "nA", "µA", "mA", "A", "cps")
)
}
\arguments{
\item{dataset}{a data frame with columns \code{species}, \code{channel}, and an
\verb{intensity.<unit>} column (e.g. \code{intensity.mV}, \code{intensity.nA}).}

\item{resistors}{a data frame with columns \code{species}, \code{channel}, and
\code{resistance.Ohm}. Required when converting between voltage and
current/CPS; ignored otherwise. If a \code{config} column is present in both
\code{dataset} and \code{resistors} it is included in the join key.}

\item{units}{target unit, one of \code{"mV"}, \code{"V"}, \code{"fA"}, \code{"pA"}, \code{"nA"},
\code{"\\u00b5A"}, \code{"mA"}, \code{"A"}, \code{"cps"}.}
}
\value{
the \code{dataset} with its \verb{intensity.*} column converted to the target
\code{units} and renamed accordingly (e.g. \code{intensity.mV})
}
\description{
Note: this function is rarely called directly, it's run as part of ir_aggregate_isofiles to standardize the trace/cycle/scan datasets before aggregation.
}
\details{
Converts the intensity column of a dataset between voltage, current, and
count-per-second units using \code{A = CPS * e} and \code{V = A * R}. Automatically
detects the source unit from any \verb{intensity.<unit>} column present in
\code{dataset}. Joins \code{resistors} by \code{species} and \code{channel} (plus \code{config} when
present in both) only when the conversion path crosses the A/V boundary.
Returns \code{dataset} with the source intensity column replaced by
\verb{intensity.<units>}.
}
