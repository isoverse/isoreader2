% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find.R
\name{ir_find_isofiles}
\alias{ir_find_isofiles}
\alias{ir_find_continuous_flow}
\alias{ir_find_dual_inlet}
\alias{ir_find_scans}
\title{Find isodat files}
\usage{
ir_find_isofiles(
  folder,
  types = c("dxf", "cf", "caf", "did", "scn"),
  pattern = NULL,
  recursive = TRUE
)

ir_find_continuous_flow(folder, pattern = NULL, recursive = TRUE)

ir_find_dual_inlet(folder, pattern = NULL, recursive = TRUE)

ir_find_scans(folder, pattern = NULL, recursive = TRUE)
}
\arguments{
\item{folder}{path to a folder with isodat files, or a character vector of folder paths}

\item{types}{file extensions to include (without leading dot), default is all supported types: \code{c("dxf", "cf", "caf", "did", "scn")}}

\item{pattern}{provide a name pattern to find only specific files}

\item{recursive}{whether to find files recursively}
}
\value{
a sorted character vector of unique file paths (\code{.json} sidecar suffixes are stripped so each file appears once regardless of whether a sidecar exists)
}
\description{
Finds isodat files with the specified extensions in one or more folders.
}
\section{Functions}{
\itemize{
\item \code{ir_find_continuous_flow()}: finds continuous flow files (\code{.dxf}, \code{.cf})

\item \code{ir_find_dual_inlet()}: finds dual inlet files (\code{.did}, \code{.caf})

\item \code{ir_find_scans()}: finds scan files (\code{.scn})

}}
\examples{
ir_find_continuous_flow(system.file("extdata", package = "isoreader2"))
ir_find_dual_inlet(system.file("extdata", package = "isoreader2"))
ir_find_scans(system.file("extdata", package = "isoreader2"))
}
