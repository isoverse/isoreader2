% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{ir_filter_for}
\alias{ir_filter_for}
\alias{ir_filter_for_continuous_flow}
\alias{ir_filter_for_dual_inlet}
\alias{ir_filter_for_scans}
\title{Filter isofiles by measurement type}
\usage{
ir_filter_for_continuous_flow(isofiles)

ir_filter_for_dual_inlet(isofiles)

ir_filter_for_scans(isofiles)
}
\arguments{
\item{isofiles}{a collection of isofiles from \code{\link[=ir_read_isofiles]{ir_read_isofiles()}}
(\code{ir_isofiles}) or datasets aggregated from \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}}
(\code{ir_aggregated_data})}
}
\value{
the \code{isofiles} object filtered to the requested measurement type
}
\description{
Convenience wrappers around \code{\link[=ir_filter_metadata]{ir_filter_metadata()}} that keep only the files of
a single measurement type (using the metadata \code{type} column): continuous flow
(\code{"cf"}), dual inlet (\code{"di"}), or scan (\code{"scan"}). Like \code{\link[=ir_filter_metadata]{ir_filter_metadata()}}
they work on both \code{ir_isofiles} (from \code{\link[=ir_read_isofiles]{ir_read_isofiles()}}) and
\code{ir_aggregated_data} (from \code{\link[=ir_aggregate_isofiles]{ir_aggregate_isofiles()}}) objects, cascade to the
other datasets, and drop any file whose metadata ends up empty.
}
\details{
Files whose metadata has no \code{type} column (e.g. a file that errored during
reading) never match and are dropped.
}
\section{Functions}{
\itemize{
\item \code{ir_filter_for_continuous_flow()}: keep only continuous flow files (\code{type == "cf"})

\item \code{ir_filter_for_dual_inlet()}: keep only dual inlet files (\code{type == "di"})

\item \code{ir_filter_for_scans()}: keep only scan files (\code{type == "scan"})

}}
